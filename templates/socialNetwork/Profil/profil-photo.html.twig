{{ form_start(form_profil_photo, {'attr': {'action': path('footballer_photo_profil_submission')} }) }}
    {{ form_row(form_profil_photo.photoProfil, {'label': false,'attr': {'accept': 'image/jpeg,image/jpg,image/png'}}) }}
{{ form_end(form_profil_photo) }}
{{ form_start(form_cover_photo, {'attr': {'action': path('footballer_photo_cover_submission')} }) }}
    {{ form_row(form_cover_photo.coverPhoto, {'label': false,'attr': {'accept': 'image/jpeg,image/jpg,image/png'}}) }}
{{ form_end(form_cover_photo) }}
