{% extends 'socialNetwork/base.html.twig' %}
{% block body %}

  <div class="container">
    <div class="timeline">
      {% include('socialNetwork/inc/view/timeline-footballer.html.twig') %}
      <div id="page-contents">
        <div class="row">
          <div class="col-md-3">
            {% include('socialNetwork/inc/view/menu-profil.html.twig') %}
          </div>
          <div class="col-md-9">
            <div class="edit-career-container">
              <div class="block-title">
                <h4 class="grey">Vidéos</h4>
              </div>
              <div class="edit-block">
                {% for video in videos %}
                  <div class="grid-item col-md-6 col-sm-6">
                    <p><strong>{{ video.title }}</strong></p>
                    <p>{{ video.description }}</p>
                    <div class="media-grid">
                      <div>
                        {% if video.internalLink is not null %}
                        <video controls width="320" height="200">
                          <source src="{{ asset(footballer_video_directory~'/'~footballer.user.account.id~'/'~video.internalLink) }}" type="video/mp4">
                        </video>
                          {% else %}
                            <iframe width="320" height="200" src="//{{ video.externalLink }}" allowfullscreen></iframe>
                        {% endif %}
                      </div>

                      <!--Popup-->
{#                      <div class="modal fade" id="modal-{{ video.id }}" tabindex="-{{ video.id }}" role="dialog" aria-hidden="true">#}
{#                        <div class="modal-dialog modal-lg">#}
{#                          <div class="modal-content">#}
{#                            <div class="post-content">#}
{#                              <div class="post-container">#}
{#                                <div class="post-detail">#}
{#                                  <div class="user-info">#}
{#                                    <h5>{{ video.title }}</h5>#}
{#                                    <p class="text-muted">{{ video.description }}</p>#}
{#                                  </div>#}
{#                                </div>#}
{#                                <div class="video-wrapper">#}
{#                                  {% if video.internalLink is not null %}#}
{#                                  <video controls>#}
{#                                    <source src="{{ asset(footballer_video_directory~'/'~footballer.user.account.id~'/'~video.internalLink) }}" type="video/mp4">#}
{#                                  </video>#}
{#                                  {% else %}#}
{#                                    <iframe width="800" height="450" src="//{{ video.externalLink }}" allowfullscreen></iframe>#}
{#                                  {% endif %}#}
{#                                </div>#}
{#                              </div>#}
{#                            </div>#}
{#                          </div>#}
{#                        </div>#}
{#                      </div><!--Popup End-->#}

                    </div>
                  </div>
                {% endfor %}
              </div>
            </div>
          </div>
          <div class="col-md-2 static">

          </div>
        </div>
      </div>
    </div>
  </div>
{% endblock %}
{% block javascripts %}
  <script>
    $('#button-youtube').click(function (){

      if($(this).hasClass('btn-danger')){
        $(this).removeClass('btn-danger');
        $('#block-youtube').hide();
        $('#block-internallink').show();

      }else{
        $(this).addClass('btn-danger');
        $('#block-youtube').show();
        $('#block-internallink').hide();

      }
    })

    $.ajax({
      url : "{{ path('footballer_check_friend',{id:footballer.id}) }}",
      type : 'POST',
      success : function (serverResponse) {
        if(serverResponse.result == 0){
          $('.add-footballer').show();
        }
        if(serverResponse.result == 1){
          $('.add-footballer').show();
          $('.add-footballer i').css('color','green');
        }
      }
    })
  </script>
{% endblock %}
